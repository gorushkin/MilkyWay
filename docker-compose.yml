version: '3'
services:
  db:
    container_name: db
    ports:
       - "5432:5432"
    environment:
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_USER=${POSTGRES_USER}
    image: postgres:14.5-alpine
  node:
    container_name: bot
    build:
      context: .
    restart: on-failure
    ports:
      - ${PORT_BACK}:${PORT_BACK}
    env_file: .env
    networks:
      - milkyway
networks:
  milkyway:
    driver: bridge
